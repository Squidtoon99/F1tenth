# Build-system only. Package metadata lives in setup.py so this same directory can
# also be built by colcon as an ament_python package (dual pip + ament package).
# Install editable for training with:  pip install -e libs/f1tenth_contract
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
