# particle_filter (vendored runtime slice)

Vendored from `f1tenth/particle_filter` with on-car edits. Provenance and the list
of dropped upstream paths (deprecated/, docs/, maps/, launch/, rviz/, test/) are in
[`../VENDORED.md`](../VENDORED.md).

Launch and map_server wiring live in `f1tenth_localization`; per-car maps mount at
`/config/maps`.
