# Default training configuration. Scaffold placeholder.
# Migration note: port the nested DEFAULT_CONFIG (obs / env / reward / model /
# selfplay) from F1tenth-Genesis config.py here.

env:
  num_envs: 512
  control_hz: 10

model:
  # e.g. gamma, n_step, batch_size, hidden sizes ...
  batch_size: 1024

reward: {}
selfplay:
  enabled: false
