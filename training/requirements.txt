# Default training requirements.
#
# Platform-split so local macOS installs and the Linux/GPU training box each get a
# working torch build:
#   - macOS (Apple Silicon):   pip install -r requirements-mac.txt
#   - Linux + NVIDIA GPU:      pip install -r requirements-gpu.txt
#
# This file defaults to the macOS set (the local dev/test target).
-r requirements-mac.txt
