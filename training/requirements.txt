# RL training dependencies (pure Python; run locally or on HPC).
# NOTE: kept separate from the ROS/car image on purpose.

# Shared observation/action contract, installed EDITABLE so obs-space changes need
# no rebuild/reinstall during training.
-e ../libs/f1tenth_contract

# Simulator for synthetic-data generation + training.
genesis-world

# Core ML stack.
torch
torchvision
numpy

# Experiment tracking / utilities.
wandb
pandas
joblib
