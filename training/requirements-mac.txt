# RL training dependencies for macOS (Apple Silicon / arm64).
#
# The standalone trainer runs NATIVELY on macOS (not in the amd64 ROS container),
# using the torch MPS/Metal or CPU backend. This installs current arm64 wheels.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-mac.txt

# Shared observation/action contract, installed EDITABLE so obs-space changes need
# no rebuild/reinstall during training.
-e ../libs/f1tenth_contract

# Core ML stack (arm64 wheels; torch selects the MPS/Metal or CPU backend).
torch
warp-lang==1.15.0
numpy

# Environment + trainer runtime deps.
requests
tqdm
python-dotenv
rerun-sdk
wandb

# Eval rollout visualization (eval_visualize.py / eval_viz.py): live view via
# rerun-sdk above; mp4 encoding via opencv + imageio(ffmpeg).
opencv-python
imageio[ffmpeg]
